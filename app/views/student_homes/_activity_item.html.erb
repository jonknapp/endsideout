<li id="<%= dom_id(link, :activity) %>" class="flex items-center gap-2 text-sm py-1">
  <span class="badge badge-outline badge-sm capitalize"><%= link.link_type %></span>
  <span class="opacity-70"><%= link.title %></span>
</li>
